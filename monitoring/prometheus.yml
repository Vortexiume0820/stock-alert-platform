global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "stock-alert-api"
    static_configs:
      - targets: ["api:8000"]

rule_files:
  - "alert_rules.yml"
  - "slo_rules.yml"
alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]